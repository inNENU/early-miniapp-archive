'use strict';

const fs = require('fs');

const testJSON = path => {
  /** 文件夹列表 */
  const forderList = fs.readdirSync(path);

  // 写入关键词内容，读出 page 目录下的文件夹
  forderList.forEach(forder => {});
};

module.exports = {};
