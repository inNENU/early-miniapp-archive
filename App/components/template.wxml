<!-- all -->
<template name="all">
  <block wx:if="{{!page}}">
    <view class="{{T}} {{nm?'nm':''}} commonLoading">
      <view class="backBtn" style="top:27.5px;" hover-class="buttonHover" hover-stay-time="100" catchtap="cA" data-action="back">
        <image class="backIcon" src="/icon/back.svg" />
      </view>
      <view class="loadingIconCtn">
        <image class="loadingIcon loading" src="/image/logo.png" />
        <view class="loadingShadow" />
        <view class="loadingText">加载中...</view>
      </view>
    </view>
  </block>
  <block wx:else>
    <view class="{{T}} {{page[0].grey?'grey':''}} {{nm?'nm':''}}">
      <!-- share组件 -->
      <share config="{{page[0]}}" />
      <block wx:for="{{page}}" wx:key="id">
        <foot config="{{item}}" wx:if="{{item.tag==='foot'}}" />
        <grid config="{{item}}" wx:elif="{{item.tag==='grid'}}" />
        <doc config="{{item}}" wx:elif="{{item.tag==='doc'}}" />
        <phone config="{{item}}" wx:elif="{{item.tag==='phone'}}" />
        <intro config="{{item}}" wx:elif="{{item.tag==='intro'}}" />
        <gzh config="{{item}}" wx:elif="{{item.tag==='gzh'}}" />
        <media config="{{item}}" wx:elif="{{item.tag==='media'}}" />
        <template wx:else is="{{item.tag}}" data="{{...item,T}}" />
      </block>
    </view>
  </block>
</template>
<template name="all2">
  <share config="{{page[0]}}" />
  <block wx:if="{{T}}" wx:for="{{page}}" wx:key="id">
    <foot config="{{item}}" wx:if="{{item.tag==='foot'}}" />
    <grid config="{{item}}" wx:elif="{{item.tag==='grid'}}" />
    <doc config="{{item}}" wx:elif="{{item.tag==='doc'}}" />
    <phone config="{{item}}" wx:elif="{{item.tag==='phone'}}" />
    <intro config="{{item}}" wx:elif="{{item.tag==='intro'}}" />
    <gzh config="{{item}}" wx:elif="{{item.tag==='gzh'}}" />
    <media config="{{item}}" wx:elif="{{item.tag==='media'}}" />
    <template wx:else is="{{item.tag}}" data="{{...item,T}}" />
  </block>
</template>
<!-- ******************************************
Here are common template used by all theme

Version: V1.0
Author: Mr.Hope
********************************************* -->
<!-- 标题 -->
<template name="title">
  <view class="{{T}}Title" style="{{style?style:''}}">{{text}}</view>
</template>
<!-- 列表 -->
<template name="list">
  <view wx:if="{{head!==false}}" class="commonhead">{{head}}</view>
  <view class="Lctn">
    <block wx:for="{{content}}" wx:for-item="List" wx:key="text">
      <block wx:if="{{!List.hidden}}">
        <!-- 导航链接 -->
        <view wx:if="{{List.url}}" id="{{List.id}}" class="listCtn" hover-class="buttonHover" hover-stay-time="100" catchtap="cA" data-action="navigate" data-url="{{List.url}}" data-aim="{{List.aim}}">
          <view class="list-Container">
            <image wx:if="{{List.icon}}" class="listIcon" src="{{List.icon}}" data-icon="true" />
            <view class="LinkText">{{List.text}}</view>
            <view class="linkDesciption access">{{List.desc}}</view>
          </view>
        </view>
        <!-- 按钮、嵌入选择器与滑块 -->
        <button wx:elif="{{List.inlay||List.button||List.slider}}" id="{{List.id}}" class="listCtn" hover-class="buttonHover" hover-stay-time="100" catchtap="{{List.pickerValue?List.picker?List.picker:'cA':List.button?List.button:List.slider?List.slider:''}}" data-id="{{List.id}}" data-action="picker" plain="true" disabled="{{List.disabled}}">
          <view class="list-Container">
            <image wx:if="{{List.icon}}" class="listIcon" src="{{List.icon}}" data-icon="true" />
            <!-- 按钮文字为蓝色 -->
            <view class="LinkText" style="{{List.button?'color:#3478f7;':''}}">{{List.text}}</view>
            <!-- 显示滑块值 -->
            <view wx:if="{{List.slider}}" class="linkDesciption">{{List.value}}</view>
            <!-- 显示行内选择器值 -->
            <view wx:elif="{{List.pickerValue}}" class="linkDesciption">
              <block wx:if="{{List.single}}">{{List.value}}</block>
              <block wx:else>
                <block wx:for="{{List.value}}" wx:key="*this">{{item}}</block>
              </block>
            </view>
          </view>
        </button>
        <!-- 非嵌入选择器 -->
        <picker wx:elif="{{List.pickerValue}}" id="{{List.id}}" class="listCtn" hover-class="buttonHover" hover-stay-time="100" catch:change="{{List.picker?List.picker:'cA'}}" value="{{List.currentValue}}" range="{{List.pickerValue}}" data-id="{{List.id}}" data-action="picker">
          <view class="list-Container">
            <view class="LinkText">{{List.text}}</view>
            <view wx:if="{{List.pickerValue}}" class="linkDesciption">
              <block wx:for="{{List.value}}" wx:key="*this">{{item}}</block>
            </view>
          </view>
        </picker>
        <navigator wx:elif="{{List.navigate}}" id="{{List.id}}" class="listCtn" hover-class="buttonHover" hover-stay-time="100" url="{{List.navigate}}" openType="{{List.openType}}" target="{{List.target}}">
          <view class="list-Container">
            <image wx:if="{{List.icon}}" class="listIcon" src="{{List.icon}}" data-icon="true" />
            <view class="LinkText">{{List.text}}</view>
            <view class="linkDesciption {{List.url?'access':''}}">{{List.desc}}</view>
          </view>
        </navigator>
        <!-- 普通文字段落与开关 -->
        <view wx:else id="{{List.id}}" class="listCtn">
          <image wx:if="{{List.icon}}" class="listIcon" src="{{List.icon}}" data-icon="true" />
          <view class="list-Container">
            <view class="LinkText">
              <text selectable="true" space="ensp" decode="true">{{List.text}}</text>
            </view>
            <view class="linkDesciption">
              {{List.desc}}
              <switch wx:if="{{List.swiKey}}" checked="{{List.status}}" bindchange="{{List.Switch?List.Switch:'cA'}}" color="{{color}}" data-id="{{List.id}}" data-action="switch" />
            </view>
          </view>
        </view>
        <!-- 滑块与嵌入式选择器显示 -->
        <block wx:if="{{List.visible}}">
          <view class="link-divline"></view>
          <view wx:if="{{List.slider}}" class="sliderCtn">
            <slider min="{{List.min?List.min:'0'}}" max="{{List.max?List.max:'100'}}" value="{{List.value}}" bindchanging="{{List.slider?List.slider:''}}" step="{{List.step?List.step:'1'}}" bindchange="{{List.slider?List.slider:''}}" data-id="{{List.id}}" data-action="slider" />
          </view>
          <picker-view wx:if="{{List.pickerValue}}" indicator-class="pickSelected" class="pickerViewCtn" value="{{List.currentValue}}" bindchange="cA" data-id="{{List.id}}" data-action="picker" mask-class="pickerMask">
            <block wx:if="{{List.single}}">
              <picker-view-column class="pick-view-column">
                <view wx:for="{{List.pickerValue}}" wx:key="*this" class="pickColumn">
                  {{item}}
                </view>
              </picker-view-column>
            </block>
            <block wx:else>
              <block wx:for="{{List.pickerValue}}" wx:key="{{item[0]}}">
                <picker-view-column class="pick-view-column">
                  <view wx:for="{{item}}" wx:key="*this" class="pickColumn">{{item}}</view>
                </picker-view-column>
              </block>
            </block>
          </picker-view>
        </block>
        <view class="{{List.icon?'listIcon ':''}}link-divline" />
      </block>
    </block>
  </view>
  <view wx:if="{{foot}}" class="commonfoot">{{foot}}</view>
</template>
<!-- 轮播图组件 -->
<template name="swiper">
  <swiper class="{{Class?Class:'swiper'}}" style="{{style}}" indicator-dots="{{indicatorDots==false?'':true}}" indicator-color="{{dotColor?dotColor:'#ffffff88'}}" indicator-active-color="{{dotActiveColor?dotActiveColor:'#fff'}}" autoplay="{{autoplay==false?false:true}}" interval="{{interval?interval:'5000'}}" duration="{{duration?duration:'500'}}" circular="{{circular==false?'':true}}" vertical="{{vertical?true:''}}" previous-margin="{{preMargin?preMargin:'0px'}}" next-margin="{{nextMargin?nextMargin:'0px'}}" bindchange="{{change?change:''}}" bindanimationfinish="{{animation?animation:''}}" data-action="swiper">
    <swiper-item wx:for="{{url}}" wx:key="*this" wx:for-item="url">
      <image class="{{imgClass?imgClass:'swiperImg'}}" src="{{url}}" mode="aspectFill" />
    </swiper-item>
  </swiper>
</template>
<!-- ******************************************
Here are  different template used by diferent theme

Version: V1.0
Author: Mr.Hope
********************************************* -->
<!-- 导航栏 -->
<template name="head">
  <block wx:if="{{!hidden}}">
    <template is="{{T}}Head" data="{{title,leftText,titleDisplay,borderDisplay,shadow,action,statusBarHeight}}" />
  </block>
</template>
<!-- 文字段落 -->
<template name="p">
  <view wx:if="{{head}}" class="{{T}}head">{{head}}</view>
  <view class="P {{T}}P" style="text-align:{{align?align:''}};">
    <text style="{{style}}" selectable="true" space="ensp" decode="true">{{text}}</text>
    <block wx:if="{{src}}">
      <template is="{{T}}img" data="{{src,load,error,id,imgMode,lazy,desc}}" />
    </block>
  </view>
</template>
<!-- 图片 -->
<template name="img">
  <template is="{{T}}img" data="{{src,load,error,id,imgMode,lazy,desc}}" />
</template>
<!-- 错误提示 -->
<template name="error">
  <template is="{{T}}error" data="{{action,statusBarHeight}}" />
</template>
<!-- ******************************************
Below are  different themes template.
********************************************* -->
<!-- ******************************************
iOS theme

Version: V1.2
Author: Mr.Hope
********************************************* -->
<!-- iOS导航栏 -->
<template name="iOSHead">
  <view class="iOS-navigationBar" style="padding-top:{{statusBarHeight}}px;">
    <view wx:if="{{titleDisplay}}" class="iOS-titletext">{{title}}</view>
    <button wx:if="{{action!==true}}" class="iOS-navBack" hover-class="navBackHover" hover-stay-time="100" plain="true" catchtap="{{action==undefined?'cA':action}}" data-action="back">
      <view wx:if="{{action==undefined||action=='redirect'}}" class="iOS-backArrow" />
      <text class="iOS-nav-text">{{leftText}}</text>
    </button>
  </view>
  <view wx:if="{{borderDisplay}}" class="iOSnavDl" style="top:{{statusBarHeight+44}}px;" />
  <view class="iOS-navPlaceHolder" style="height:{{statusBarHeight+44}}px;">
    到头啦~人家被你拉得好痛阿(*´д`*) 不许你再拉人家~╭(╯^╰)╮
  </view>
  <view class="iOS-head">{{title}}</view>
</template>
<!-- iOS图片 -->
<template name="iOSimg">
  <view wx:if="{{error}}" class="iOSImgError">
    <icon type="warn" size="32" style="margin-bottom:5px;" />
    <text>图片加载失败</text>
  </view>
  <view wx:elif="{{!load}}" class="iOSImgError">
    <image class="loadingImg" mode="{{imgMode}}" src="/icon/loading.svg" lazy-load="{{true}}" />
    <text>加载中...</text>
  </view>
  <image id="{{id}}" class="iOS-img" style="{{load?'':'width:1rpx;height:1rpx;'}}" mode="{{imgMode}}" lazy-load="{{lazy!==false}}" src="{{src}}" bindload="cA" binderror="cA" catch:tap="cA" data-action="img" />
  <view wx:if="{{desc}}" class="iOSN">▲{{desc}}</view>
</template>
<!-- iOS错误 -->
<template name="iOSerror">
  <view class="iOS-navigationBar" style="padding-top:{{statusBarHeight}}px;">
    <view class="iOS-titletext">页面出错</view>
    <button wx:if="{{!action}}" class="iOS-navBack" hover-class="navBackHover" hover-stay-time="100" plain="true" catch:tap="cA" data-action="back">
      <view class="iOS-backArrow" />
      <text class="iOS-nav-text">返回</text>
    </button>
  </view>
  <view class="iOSnavDl" style="top:{{statusBarHeight+44}}px;" />
  <view class="iOS-navPlaceHolder" style="height:{{statusBarHeight+44}}px;">
    到头啦~人家被你拉得好痛阿(*´д`*) 不许你再拉人家~╭(╯^╰)╮
  </view>
  <view class="errorCtn">
    <icon type="warn" size="80" />
    <view class="errorText">很抱歉，界面遇到了问题!</view>
    <view class="errorDesc">
      <text>
        这位同学你好，我是Mr.Hope，很抱歉让你看到如下错误信息，错误原因有几下几种可能：\n\n1. 服务器负载过重；\n2. 设备可能无法访问互联网； \n3. 该页面可能尚未被制作；\n\n请同学稍后重试~\n由于服务器一天不足一元钱，配置不是很好，您可以点击界面底部的捐赠按钮支持Mr.Hope，帮助Mr.Hope升级服务器配置。或者您也可以打赏Mr.Hope，感谢同学对Mr.Hope的支持~。
      </text>
    </view>
  </view>
  <view class="donateCtn">
    <navigator class="donateButton" url="/settings/donate">捐赠</navigator>
  </view>
</template>
<!-- ******************************************
Android theme

Version: V1.0
Author: Mr.Hope
********************************************* -->
<!-- Android导航栏 -->
<template name="AndroidHead">
  <view class="Android-navigationBar {{shadow?'AndroidNavShadow':''}}" style="padding-top:{{statusBarHeight}}px;">
    <view class="Android-titletext">{{title}}</view>
    <button wx:if="{{!action}}" class="Android-navBack" hover-class="navBackHover" hover-stay-time="100" plain="true" catch:tap="{{action==undefined?'cA':action}}" data-action="back">
      <view wx:if="{{!action}}" class="Android-backArrow" />
      <view class="Android-leftText">{{leftText}}</view>
    </button>
  </view>
  <view class="Android-navPlaceHolder" style="height:{{statusBarHeight+44}}px;">
    到头啦~人家被你拉得好痛阿(*´д`*) 不许你再拉人家~╭(╯^╰)╮
  </view>
  <view wx:if="{{desc}}" class="Android-head">
    <view class="Android-title">{{title}}</view>
    <view wx:if="{{desc}}" class="Android-desc">{{desc}}</view>
  </view>
</template>
<!-- Android图片 -->
<template name="Androidimg">
  <view wx:if="{{error}}" class="imgError">
    <icon type="warn" style="margin-right:5px;" />
    <text class="Grey">图片加载失败</text>
  </view>
  <view wx:elif="{{!load}}" class="imgError">
    <image class="loadingImg" mode="{{imgMode}}" src="/icon/loading.svg" />
    <text class="Grey">加载中...</text>
  </view>
  <image id="{{id}}" class="Android-img" style="{{load?'':'width:1rpx;height:1rpx;border:none;box-shadow:none;'}}" mode="{{imgMode}}" lazy-load="{{lazy!==false}}" src="{{src}}" bindload="cA" binderror="cA" catch:tap="cA" data-action="img" />
  <view wx:if="{{desc}}" class="AndroidN">▲{{desc}}</view>
</template>
<!-- Android错误页面 -->
<template name="Androiderror">
  <view class="Android-navigationBar" style="padding-top:{{statusBarHeight}}px;">
    <view class="Android-titletext">页面出错</view>
    <button wx:if="{{!top}}" class="Android-navBack" hover-class="navBackHover" hover-stay-time="100" plain="true" catch:tap="cA" data-action="back">
      <view class="Android-backArrow" />
    </button>
  </view>
  <view class="Android-navPlaceHolder" style="height:{{statusBarHeight+44}}px;">
    <text>到头啦~人家被你拉得好痛阿(*´д`*) 不许你再拉人家~╭(╯^╰)╮</text>
  </view>
  <view class="errorCtn">
    <icon type="warn" size="80" />
    <view class="errorText">很抱歉，界面遇到了问题!</view>
    <view class="errorDesc">
      <text>
        这位同学你好，我是Mr.Hope，很抱歉让你看到如下错误信息，错误原因有几下几种可能：\n\n1. 服务器负载过重；\n2. 设备可能无法访问互联网； \n3. 该页面可能尚未被制作；\n\n请同学稍后重试~\n由于服务器一天不足一元钱，配置不是很好，您可以点击界面底部的捐赠按钮支持Mr.Hope，帮助Mr.Hope升级服务器配置。或者您也可以打赏Mr.Hope，感谢同学对Mr.Hope的支持~。
      </text>
    </view>
  </view>
  <view class="donateCtn">
    <navigator class="donateButton" url="/settings/donate">捐赠</navigator>
  </view>
</template>
<!-- ******************************************
NENU theme

Version: V1.0
Author: Mr.Hope
********************************************* -->
<template name='NENUHead'>
  <block wx:if="{{action==undefined}}">
    <view class="backBtn {{borderDisplay?'fadeIn':'fadeOut'}}" style="top:{{statusBarHeight+5}}px;" hover-class='buttonHover' catch:tap="cA" data-action='back'>
      <image class='backIcon' src="/icon/back.svg" />
    </view>
    <view class="NENUnav {{titleDisplay?'fadeOut':'fadeIn'}}" style="background-color:{{color}};padding-top:{{statusBarHeight}}px;">
      <button class="iOS-navBack" hover-class="navBackHover" style='position:absolute;left:0;' plain='true' catch:tap="cA" data-action='back'>
        <view wx:if="{{action==undefined||action=='redirect'}}" class='iOS-backArrow' style='border-color:#fff;' />
        <text class='iOS-nav-text' style='color:#fff;'>{{leftText}}</text>
      </button>
      <view class='NENU-navText'>{{title}}</view>
    </view>
  </block>
  <block wx:else>
    <view class="NENUnav" style="background-color:{{color}};padding-top:{{statusBarHeight}}px;">
      <button wx:if="{{action!==true}}" class="iOS-navBack" hover-class="navBackHover" style='position:absolute;left:0;' plain='true' catch:tap="{{action}}">
        <view wx:if="{{action=='redirect'}}" class='iOS-backArrow' style='border-color:#fff;'></view>
        <text class='iOS-nav-text' style='color:#fff;'>{{leftText}}</text>
      </button>
      <view class='NENU-navText'>{{title}}</view>
    </view>
  </block>
  <view style="width:100%;height:{{statusBarHeight+40}}px;"></view>
</template>
<template name='NENUimg'>
  <view wx:if="{{error}}" class='iOSImgError'>
    <icon type="warn" size='32' style='margin-bottom:5px;' />
    <text>图片加载失败</text>
  </view>
  <view wx:elif="{{!load}}" class='iOSImgError'>
    <image class='loadingImg' mode="{{imgMode}}" src="/icon/loading.svg" lazy-load="{{true}}" />
    <text>加载中...</text>
  </view>
  <image id="{{id}}" class='iOS-img' style="{{load?'':'width:1rpx;height:1rpx;'}}" mode="{{imgMode}}" lazy-load="{{lazy!==false}}" src="{{src}}" bindload="cA" catch:error="cA" catch:tap="cA" data-action='img' />
  <view wx:if="{{desc}}" class='iOSN'>▲{{desc}}</view>
</template>
<!-- NENU错误页面 -->
<template name='NENUerror'>
  <view class='NENUnav' style="background-color:{{color}};padding-top:{{statusBarHeight}}px;">
    <view class='NENU-navText'>页面出错</view>
  </view>
  <view style='width:100%;background-color:{{color}};height:{{statusBarHeight+40}}px;' />
  <view class='backBtn' style="top:{{statusBarHeight+5.5}}px;" hover-class='buttonHover' hover-stay-time='100' catch:tap='cA' data-action='back'>
    <image class='backIcon' src='/icon/back.svg' />
  </view>
  <view class="errorCtn">
    <icon type="warn" size="80" />
    <view class="errorText">很抱歉，界面遇到了问题!</view>
    <view class="errorDesc">
      <text>
        这位同学你好，我是Mr.Hope，很抱歉让你看到如下错误信息，错误原因有几下几种可能：\n\n1. 服务器负载过重；\n2. 设备可能无法访问互联网； \n3. 该页面可能尚未被制作；\n\n请同学稍后重试~\n由于服务器一天不足一元钱，配置不是很好，您可以点击界面底部的捐赠按钮支持Mr.Hope，帮助Mr.Hope升级服务器配置。或者您也可以打赏Mr.Hope，感谢同学对Mr.Hope的支持~。
      </text>
    </view>
  </view>
  <view class="donateCtn">
    <navigator class="donateButton" url="/settings/donate">捐赠</navigator>
  </view>
</template>
<template name='NENUinfo'>
  <view class='NENUInfoCtn'>
    <view class='NENUInfoHead' style='backgroundcolor:{{page[0].color}};'>
      <button wx:if="{{!login}}" class='NENULoginBtn' style='backgroundcolor:{{page[0].color}};' open-type='getUserInfo'>
        立即登录
      </button>
      <view wx:if="{{login}}" class='nickCtn'>
        <image class='NENUnickHead' />
        <view class="NENUnickname" />
      </view>
    </view>
  </view>
</template>