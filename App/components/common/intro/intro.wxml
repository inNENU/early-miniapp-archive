<view class="introBox">
  <image class="introBg" src="{{config.src}}" mode="aspectFill" />
  <view class="introCtn">
    <image class="introIcon" src="{{config.src}}" />
    <view class="introName">{{config.name}}</view>
    <view class="introDesc">{{config.desc}}</view>
  </view>
</view>