<view wx:if="{{config.shareable||config.contact||config.feedback}}" class="iconCtn" style="top:{{top}}px;left:{{left}}px;" hover-class="iconHover" hover-stay-time="200" catch:touchstart="move" catch:touchmove="move" catch:touchend="move" aria-role="button" aria-label="分享按钮">
  <image class="icon" src="share.svg" />
</view>
<view wx:if="{{menuDisplay}}" class="mask {{menuDisplay?'fadeIn':'fadeOut'}}" style="top:{{menuDisplay?'0':'100%'}};" />
<view class="layer {{menuDisplay?'moveUpIn':'moveUpOut'}}" catch:tap="cancel">
  <view class="shareBtnList">
    <button wx:if="{{config.shareable}}" class="shareBtn" hover-class="hover" hover-start-time="0" open-type="share">
      分享{{env==='qq'?'给好友/分享到空间':'到微信'}}
    </button>
    <button wx:if="{{config.shareable}}" class="shareBtn" hover-class="hover" hover-start-time="0" bind:tap="download">
      保存页面二维码
    </button>
    <button wx:if="{{config.contact}}" class="shareBtn" hover-class="hover" hover-start-time="0" open-type="contact" bind:tap="contact">
      联系开发者
    </button>
    <button wx:if="{{config.feedback}}" class="shareBtn" hover-class="hover" hover-start-time="0" open-type="feedback">
      意见反馈
    </button>
    <button class="cancelBtn" hover-class="hover">取消</button>
  </view>
</view>