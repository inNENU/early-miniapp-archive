<import src="/components/template" />
<view class="{{T}} grey {{nm?'nm':''}} {{nm? 'dark': 'light'}}">
  <template is="head" data="{{...head,T}}" />
  <search words="{{words}}" catch:searching="searching" catch:search="search" />
  <template is="body" data="{{page,T}}" />
</view>