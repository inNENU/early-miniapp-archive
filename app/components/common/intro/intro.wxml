<view class="intro-wrapper">
  <!-- 高斯模糊背景 -->
  <image class="background" src="{{config.src}}" mode="aspectFill" />
  <view class="intro">
    <!-- 图标 -->
    <image class="icon" src="{{config.src}}" />
    <!-- 名称 -->
    <view class="name">{{config.name}}</view>
    <!-- 详情介绍 -->
    <view class="desc">{{config.desc}}</view>
  </view>
</view>