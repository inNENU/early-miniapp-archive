export declare enum SignalType {
    Continue = 0,
    Break = 1,
    Return = 2,
}
export default class Signal {
    readonly type: SignalType;
    readonly value: any;
    static Continue(label?: string): Signal;
    static Break(label?: string): Signal;
    static Return(value?: any): Signal;
    static isSignal(signal: any): boolean;
    static isContinue(signal: any): boolean;
    static isBreak(signal: any): boolean;
    static isReturn(signal: any): boolean;
    constructor(type: SignalType, value?: any);
}
