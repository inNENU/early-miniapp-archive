import { Value } from './value';
export default class Sandbox {
    private sandbox;
    constructor(sandbox: {
        [key: string]: any;
    });
    get(name: string, autoDeclare?: boolean): Value | undefined;
    declare(name: string, value?: any): void;
}
